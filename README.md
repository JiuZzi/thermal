# 红外图像轮廓生成：ISNet、UIU-Net 与结构约束的递进式双数据集实验方案

## 一、研究目标

本实验研究单波段红外图像中的目标掩膜和轮廓提取问题。总体目标不是直接恢复真实可见光纹理，而是从红外图像中提取结构可靠、边界连续、目标区域完整的表示。

模型输入为单通道红外图像：

\[
I_{IR}\in\mathbb{R}^{1\times H\times W}
\]

模型主要输出两类结果：

1. 目标前景掩膜；
2. 目标轮廓图。

推荐的最终任务定义为：

\[
I_{IR}\rightarrow \{M_{target}, C_{contour}\}
\]

其中，掩膜用于保证目标区域完整，轮廓用于保证边界精细。

需要明确：单张红外图像中如果人物与背景的温度、发射率和辐射强度几乎一致，图像本身可能没有足够信息恢复真实边界。因此本研究的目标应表述为“提高复杂红外背景下目标结构与轮廓预测的可靠性”，而不是保证恢复绝对真实的不可见轮廓。

---

## 二、为什么使用两个数据集

### 1. IRSTD-1k：验证红外小目标和形状建模能力

IRSTD-1k 是 ISNet 使用的重要红外小目标数据集，具有以下特点：

- 单模态红外图像；
- 像素级目标标注；
- 目标尺寸较小；
- 目标形状具有多样性；
- 背景杂波较丰富。

它最适合回答：

> 一个模块是否真的能够改善微弱小目标的检测、分割和轮廓质量？

因此，E0–E8 的主要消融实验首先在 IRSTD-1k 上完成。

### 2. MSRS：验证多尺度和复杂场景泛化能力

MSRS 包含红外和可见光图像，并提供语义分割标签，场景中既有远处小目标，也有近处较大目标，例如行人、车辆和道路区域。

它适合回答：

> 在小目标之外，模型能否处理不同尺寸、不同类别和更复杂道路场景中的目标轮廓？

MSRS 不应替代 IRSTD-1k 的小目标专项实验，而应作为第二阶段的扩展验证数据集。

---

## 三、统一任务和标签处理

### 1. IRSTD-1k 标签

IRSTD-1k 的目标标注通常可以直接转换为二值掩膜：

\[
Y_{mask}(x,y)\in\{0,1\}
\]

### 2. MSRS 标签

MSRS 原始标签是多类别语义分割标签。第一阶段建议将所有非背景类别合成为前景：

\[
Y_{mask}=(Y_{semantic}>0)
\]

后续如果需要研究类别差异，再单独评估行人、车辆等类别。

### 3. 轮廓标签

建议从完整掩膜生成轮廓，而不是直接人工标注一像素轮廓：

\[
Y_{contour}=Y_{mask}-Erode(Y_{mask})
\]

这样可以同时监督区域和边界。

### 4. 可选 SDF 标签

对于结构约束实验，可以额外生成有符号距离场：

- 目标内部为负值；
- 目标边界为 0；
- 背景为正值。

记为：

\[
Y_{sdf}(x,y)
\]

---

## 四、固定实验条件

为了保证每一阶段的比较公平，除模型模块外，其余条件必须尽量保持一致：

- 数据集划分一致；
- 输入尺寸一致；
- batch size 一致；
- 优化器一致；
- 初始学习率一致；
- 训练轮数一致；
- 数据增强一致；
- 随机种子一致；
- 模型选择策略一致；
- 测试集只用于最终评估。

建议从训练集划分 validation 集，例如：

```text
训练集：80%
验证集：20%
测试集：官方测试集或固定测试集
```

不要使用 test 集频繁选择模型，否则测试结果会产生偏差。

建议每组实验使用至少 3 个随机种子，例如：

```text
42、123、3407
```

最终报告平均值和标准差。

---

## 五、八个递进阶段

八个阶段首先在 IRSTD-1k 上完成。每一阶段只增加一个主要因素，观察指标和可视化结果的变化。

### E0：基础 U-Net

结构：

```text
红外图像
→ 编码器
→ 瓶颈层
→ 解码器
→ Mask Head
```

损失：

\[
L_{E0}=L_{BCE}+L_{Dice}
\]

作用：

- 建立最低基线；
- 观察普通编码器—解码器对红外目标的基本能力；
- 为后续 ISNet、UIU-Net 和结构约束提供参照。

重点问题：

- 小目标是否漏检；
- 轮廓是否断裂；
- 背景杂波是否产生误检；
- 预测是否偏向大区域。

### E1：原始 ISNet

结构：

```text
红外图像
→ ISNet 编码器
→ TFD-inspired Edge Block
→ TOAA
→ 解码器
→ 目标分割图
```

核心模块：

1. TFD-inspired Edge Block：利用有限差分启发增强边缘变化；
2. TOAA：沿行和列两个方向聚合注意力，建模目标形状特征；
3. 多层特征融合：同时利用低层边缘和高层语义。

作用：验证 ISNet 针对红外小目标的专门设计是否优于基础 U-Net。

重点指标：小目标 Recall、F1、IoU、Boundary F1 和误检率。

### E2：ISNet + Mask/Contour 双分支

在 ISNet 的基础上增加轮廓预测头：

```text
ISNet 特征
   ├── Mask Head
   └── Contour Head
```

输出：

\[
P_m=MaskHead(F)
\]

\[
P_c=ContourHead(F)
\]

损失：

\[
L_{E2}=L_{mask}+\lambda_cL_{contour}
\]

其中：

\[
L_{mask}=L_{BCE}(P_m,Y_m)+L_{Dice}(P_m,Y_m)
\]

\[
L_{contour}=L_{BCE}(P_c,Y_c)+L_{Dice}(P_c,Y_c)
\]

建议初始设置：

```text
λc = 1.0
```

作用：验证显式轮廓监督是否改善边界质量。

注意：E2 主要可能改善轮廓精度，不一定显著提高小目标召回率。

### E3：E2 + Edge Loss

计算红外图像梯度：

\[
G_I=\sqrt{I_x^2+I_y^2}
\]

计算预测掩膜梯度：

\[
G_M=\sqrt{M_x^2+M_y^2}
\]

加入软边界约束：

\[
L_{edge}=\|G_M-G_I\|_1
\]

总损失：

\[
L_{E3}=L_{mask}+\lambda_cL_{contour}+\lambda_eL_{edge}
\]

建议：

```text
λe = 0.05～0.2
```

作用：验证红外图像中的边缘变化是否能帮助定位目标边界。

风险：道路、建筑、树木和热反射也可能具有强梯度，因此不能让预测轮廓完全复制红外梯度。

### E4：E3 + Edge Reliability

为了处理迷彩服、低对比度目标和复杂背景，构造边缘可靠性图。

局部对比度：

\[
C(x,y)=
\frac{|I(x,y)-\mu_{\mathcal{N}(x,y)}|}
{\sigma_{\mathcal{N}(x,y)}+\epsilon}
\]

可以将梯度和局部对比度组合为：

\[
R=\alpha\hat{G}_I+\beta\hat{C}
\]

实现方式有两种。

#### 方式 A：作为额外输入

```text
[红外图 I，梯度 G，局部对比度 C]
→ ISNet
→ Mask + Contour
```

#### 方式 B：作为损失权重

\[
L_{edge}=\sum_{x,y}R(x,y)|G_M(x,y)-G_I(x,y)|
\]

建议先采用方式 A，因为更容易观察模型是否真正使用了这些特征。

作用：验证边缘可靠性信息能否减少背景强边缘误检，并改善低对比度目标。

风险：可靠性公式属于人工先验，可能在一个数据集有效、换到另一个数据集失效。

### E5：E4 + SDF Structural Constraint

在 Mask Head 和 Contour Head 之外，增加 SDF Head：

```text
ISNet 特征
   ├── Mask Head
   ├── Contour Head
   └── SDF Head
```

SDF 损失：

\[
L_{SDF}=\|D_{pred}-Y_{sdf}\|_1
\]

总损失：

\[
L_{E5}=L_{mask}+\lambda_cL_{contour}+\lambda_eL_{edge}+\lambda_sL_{SDF}
\]

建议：

```text
λs = 0.05～0.2
```

作用：

- 减少轮廓断裂；
- 减少锯齿和毛刺；
- 减少目标内部孔洞；
- 提高整体形状稳定性。

SDF 主要改善“已经找到的目标的结构质量”，不能保证把完全不可见的小目标识别出来。

### E6：UIU-Net + Mask/Contour 双分支

使用 UIU-Net 作为主干，但暂时不加入 ISNet 的 TFD 和 TOAA：

```text
红外图像
→ UIU-Net 多尺度主干
→ Mask Head + Contour Head
```

损失与 E2 保持一致：

\[
L_{E6}=L_{mask}+\lambda_cL_{contour}
\]

作用：单独验证 UIU-Net 的多尺度特征提取和嵌套结构是否有助于小目标轮廓。

重点观察：

- 小目标 Recall 是否提高；
- 大目标区域是否更完整；
- 多尺度目标是否更稳定；
- 参数量和推理时间是否明显增加。

### E7：UIU-Net + ISNet Edge/TOAA

在 UIU-Net 的多尺度主干中加入 ISNet 的边缘和方向模块：

```text
UIU-Net 多尺度主干
→ TFD Edge Block
→ TOAA
→ Mask Head + Contour Head
```

作用：验证 UIU-Net 和 ISNet 是否互补。

UIU-Net 主要负责：

- 多尺度特征提取；
- 保留高分辨率细节；
- 处理不同大小目标。

ISNet 主要负责：

- 边缘增强；
- 方向关系建模；
- 目标形状提取。

风险：两个网络可能存在功能重叠，导致参数量增加、训练不稳定或过拟合。因此必须与 E6、E1 分别比较，不能直接认为组合一定有效。

### E8：UIU-Net + ISNet + Structural Constraint

最终候选模型：

```text
红外图像
→ UIU-Net 多尺度主干
→ TFD Edge Block
→ TOAA
→ Edge Reliability
→ SDF Structural Constraint
→ Mask Head
→ Contour Head
→ SDF Head
```

总损失：

\[
L_{E8}=
L_{mask}
+\lambda_cL_{contour}
+\lambda_eL_{edge}
+\lambda_sL_{SDF}
\]

如果 E4 的 Edge Reliability 没有显著提升，E8 中可以去掉它，避免无效复杂度。

E8 的主要目标不是证明“模块越多越好”，而是验证经过前面消融后留下的有效模块组合能否取得最好的综合性能。

---

## 六、IRSTD-1k 阶段的实验矩阵

建议首先完成下列实验：

```text
E0：基础 U-Net
E1：原始 ISNet
E2：ISNet + Mask/Contour 双分支
E3：E2 + Edge Loss
E4：E3 + Edge Reliability
E5：E4 + SDF Structural Constraint
E6：UIU-Net + Mask/Contour 双分支
E7：UIU-Net + ISNet Edge/TOAA
E8：UIU-Net + ISNet + Structural Constraint
```

每组实验都保存：

1. 最优模型权重；
2. 训练和验证损失曲线；
3. 测试集掩膜；
4. 测试集轮廓；
5. 原图—真实掩膜—预测掩膜—真实轮廓—预测轮廓对比图；
6. 误检、漏检和正确区域可视化；
7. ROI、边缘可靠性和 SDF 图（如果对应模块存在）。

---

## 七、从 IRSTD-1k 到 MSRS 的第二阶段

当 IRSTD-1k 实验完成后，不要直接宣布模型适合所有红外场景。需要在 MSRS 上进行迁移和泛化实验。

### MSRS 实验 A：MSRS 独立训练和测试

```text
MSRS train → 训练模型
MSRS validation → 选择模型
MSRS test → 最终评估
```

目的：验证模型面对行人、车辆、道路以及不同目标尺度时的性能。

推荐运行三类模型：

```text
基础 U-Net
最优 ISNet/结构模型
最优 UIU-Net/结构模型
```

不一定需要把所有 E0–E8 在 MSRS 上完整重复一遍。完整消融在 IRSTD-1k 上完成，MSRS 主要用于验证泛化和多尺度适应能力。

### MSRS 实验 B：IRSTD-1k 训练 → MSRS 测试

```text
IRSTD-1k train → 训练模型
MSRS test → 直接测试
```

目的：测试跨数据集泛化能力。

预计性能可能下降，因为：

- 目标类别不同；
- 目标尺度不同；
- 场景不同；
- 标签定义不同；
- 红外图像成像条件不同。

但如果结构模块在跨数据集测试中仍然保持较好的边界质量，说明它学到的可能是更通用的边缘和结构规律，而不只是记住某个数据集的亮点模式。

### MSRS 实验 C：联合训练

可将两个数据集统一为二分类前景任务，混合训练：

```text
IRSTD-1k + MSRS → 联合训练
```

但需要注意数据分布不平衡。建议：

- 使用平衡采样；
- 记录数据集来源；
- 分别报告 IRSTD-1k 和 MSRS 测试结果；
- 不要只报告混合测试集总体指标。

---

## 八、评价指标

### 1. 区域指标

\[
Precision=\frac{TP}{TP+FP}
\]

\[
Recall=\frac{TP}{TP+FN}
\]

\[
F1=\frac{2PR}{P+R}
\]

\[
IoU=\frac{TP}{TP+FP+FN}
\]

用于评价目标区域是否被完整分割。

### 2. 边界指标

建议加入：

- Boundary Precision；
- Boundary Recall；
- Boundary F1；
- Boundary IoU；
- Hausdorff Distance；
- Average Symmetric Surface Distance。

这些指标比普通 IoU 更适合评价轮廓质量。

### 3. 小目标专项指标

在 IRSTD-1k 上重点报告：

- 小目标 Recall；
- 小目标 Precision；
- 小目标 F1；
- 漏检率；
- 虚警率；
- 每张图平均误检数。

### 4. 尺度分组指标

在 MSRS 上按目标面积划分：

```text
small
medium
large
```

可以使用 COCO 风格阈值，或根据 MSRS 的目标面积分布重新设置阈值。分别计算每组目标的 Precision、Recall、F1、IoU 和 Boundary F1。

### 5. 复杂背景指标

额外统计：

- 道路边缘误检率；
- 建筑边缘误检率；
- 热反射误检率；
- 低对比度目标召回率；
- 多目标粘连率；
- 轮廓断裂率。

---

## 九、损失、指标和模型训练的关系

### 1. 参与训练的内容

真实掩膜、真实轮廓和 SDF 标签会参与损失计算：

\[
X\rightarrow P\rightarrow L(P,Y)\rightarrow
\text{反向传播}\rightarrow\text{更新参数}
\]

例如：

```text
BCE Loss
Dice Loss
Contour Loss
Edge Loss
SDF Loss
```

### 2. 主要用于人工评估的内容

以下指标通常不直接参与反向传播：

```text
Precision
Recall
F1
IoU
Boundary F1
Hausdorff Distance
```

它们主要用于：

- 选择最佳模型；
- 比较不同实验；
- 评估生成质量；
- 撰写论文结果。

Dice 需要区分：

```text
Dice 指标：用于评估
Dice Loss：用于训练
```

---

## 十、如何判断某个模块真的有效

不能只看一次实验的 F1 是否上升。每个模块至少满足以下条件之一，才可以认为具有实际价值：

1. 总体指标稳定提升；
2. Boundary F1 明显提升；
3. 小目标 Recall 提升；
4. 低对比度目标漏检减少；
5. 复杂背景误检减少；
6. 跨数据集泛化更好；
7. 在性能相近时显著降低参数量或推理时间。

如果某模块只提高总体 IoU，但小目标 Recall 和 Boundary F1 下降，就不能称为轮廓任务上的有效模块。

如果某模块提升很小且低于多个随机种子的标准差，也不能轻易声称它有效。

---

## 十一、最终推荐的论文式实验逻辑

### 第一部分：小目标专项验证

在 IRSTD-1k 上完成 E0–E8，验证：

```text
ISNet 是否优于基础 U-Net
轮廓分支是否改善边界
Edge Loss 是否改善边界定位
Edge Reliability 是否减少背景误检
SDF 是否改善连续性
UIU-Net 是否改善小目标和多尺度特征
UIU-Net 与 ISNet 是否互补
```

### 第二部分：多尺度场景验证

在 MSRS 上训练和测试最具代表性的模型，验证：

```text
模型是否能处理小、中、大目标
模型是否能处理行人和车辆等不同类别
模型是否能适应复杂道路背景
小目标优化是否损害大目标完整性
```

### 第三部分：跨数据集泛化验证

使用 IRSTD-1k 训练的模型直接测试 MSRS，或使用 MSRS 训练的模型测试其他数据集，观察：

```text
边缘模块是否具有通用性
结构约束是否依赖特定数据集
UIU-Net 是否提高尺度泛化
模型是否过度依赖某种红外成像分布
```

---

## 十二、最推荐的实际执行顺序

虽然理论上可以做 E0–E8，但实际编码和训练建议分阶段完成：

```text
第 1 步：E0 基础 U-Net
第 2 步：E1 原始 ISNet
第 3 步：E2 ISNet + Mask/Contour 双分支
第 4 步：E3 加入 Edge Loss
第 5 步：E5 直接加入 SDF 结构约束
第 6 步：E6 单独测试 UIU-Net
第 7 步：E7 测试 UIU-Net + ISNet 模块
第 8 步：E8 形成最终模型
第 9 步：在 MSRS 上进行独立训练测试
第 10 步：进行 IRSTD-1k → MSRS 跨数据集测试
```

E4 的 Edge Reliability 建议作为可选实验。如果它对 IRSTD-1k 有效但对 MSRS 明显失效，需要说明它可能是数据集相关的人工先验。

---

## 十三、预期结果及合理解释

理想情况下，可能出现以下趋势：

```text
E0：基础性能最低，但结构简单、速度快
E1：小目标召回和边界质量提升
E2：轮廓 Boundary F1 提升
E3：边界定位可能提升，但背景误检存在风险
E4：低对比度目标可能改善，也可能表现出数据集依赖
E5：轮廓断裂和锯齿减少
E6：多尺度目标和小目标特征保留改善
E7：如果互补成立，综合指标进一步提升
E8：可能得到最好的轮廓质量，但计算量和过拟合风险最高
```

也可能出现相反结果，这并不意味着实验失败。例如：

- UIU-Net 复杂度增加但 IRSTD-1k 提升很小；
- Edge Loss 增加了道路边缘误检；
- Edge Reliability 在 IRSTD-1k 有效但 MSRS 无效；
- SDF 改善边界但降低小目标 Recall；
- E8 比 E7 更复杂但没有显著提升。

这些结果同样有研究价值，因为它们能说明模块的适用范围和局限性。

---

## 十四、最终研究定位

建议将最终方法定位为：

> 面向单波段红外图像的多尺度结构感知目标轮廓提取方法。该方法以 UIU-Net 或 ISNet 为特征提取基础，通过边缘增强、方向注意力、掩膜—轮廓联合预测以及 SDF 结构约束，提高复杂背景下红外目标区域和边界的预测可靠性，并在红外小目标数据集 IRSTD-1k 与包含多尺度目标的 MSRS 数据集上验证其有效性和泛化能力。

最重要的科学问题是：

```text
红外图像中的弱热差异，能否与局部边缘、邻域连续性和全局形状先验结合，形成比普通像素分类更可靠的目标轮廓表示？
```

最终不要把结论写成“模型可以恢复真实不可见轮廓”，而应写成：

> 在可观测红外信号有限的情况下，利用多尺度特征、边缘信息和结构先验，提高目标轮廓预测的完整性、连续性和跨场景稳定性。
